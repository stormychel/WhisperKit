[project]
name = "whisperkit-scripts"
version = "0.1.0"
description = "Scripts for WhisperKit development"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.33.0",
    "urllib3>=2.7.0",
    "ruamel.yaml",
]
